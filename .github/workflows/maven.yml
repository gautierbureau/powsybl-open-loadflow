name: CI

on:
  push:
    branches:
      - 'main'
      - 'release-v**'
      - 'full-sonar-analysis-**'
  pull_request:

permissions: {}

jobs:
  build:
    name: Build OS ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    if: github.event.pull_request.head.repo.fork == false
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    env:
      # SonarCloud is only configured on the powsybl repository: on other repositories (personal forks
      # for instance) the secret is not defined and the analysis is skipped instead of failing the build.
      HAS_SONAR_TOKEN: ${{ secrets.SONAR_TOKEN != '' }}

    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      - name: Build with Maven (Ubuntu / MacOS)
        if: matrix.os != 'windows-latest'
        run: ./mvnw --batch-mode -Pjacoco install

      - name: Build with Maven (Windows)
        if: matrix.os == 'windows-latest'
        run: mvnw.cmd --batch-mode install
        shell: cmd

      - name: Skip SonarCloud analysis
        if: matrix.os == 'ubuntu-latest' && env.HAS_SONAR_TOKEN != 'true'
        run: echo "::notice::SONAR_TOKEN is not defined on this repository, skipping the SonarCloud analysis"

      - name: Run SonarCloud analysis
        if: matrix.os == 'ubuntu-latest' && env.HAS_SONAR_TOKEN == 'true'
        run: >
          ./mvnw --batch-mode -DskipTests sonar:sonar
          -Dsonar.host.url=https://sonarcloud.io
          -Dsonar.organization=powsybl-ci-github
          -Dsonar.projectKey=com.powsybl:powsybl-open-loadflow
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
